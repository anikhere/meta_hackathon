[project]
name = "supply-chain-env"
version = "1.0.0"
description = "Supply Chain Management AI Agent Environment"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
